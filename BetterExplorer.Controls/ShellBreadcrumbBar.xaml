<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BetterExplorer.Controls.ShellBreadcrumbBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BetterExplorer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    VerticalAlignment="Stretch">

    <UserControl.Resources>

        <!-- Style for breadcrumb segment buttons (no chrome, hover highlight) -->
        <Style x:Key="BreadcrumbSegmentStyle" TargetType="Button">
            <Setter Property="Background"           Value="Transparent" />
            <Setter Property="BorderBrush"          Value="Transparent" />
            <Setter Property="BorderThickness"      Value="0" />
            <Setter Property="Padding"              Value="6,0,2,0" />
            <Setter Property="VerticalAlignment"    Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontSize"             Value="12" />
            <Setter Property="FontFamily"           Value="Segoe UI Variable Text" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid x:Name="Root" Background="{TemplateBinding Background}"
                              CornerRadius="4">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="Root.Background"
                                                    Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="Root.Background"
                                                    Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for the chevron dropdown button -->
        <Style x:Key="BreadcrumbChevronStyle" TargetType="Button">
            <Setter Property="Background"           Value="Transparent" />
            <Setter Property="BorderBrush"          Value="Transparent" />
            <Setter Property="BorderThickness"      Value="0" />
            <Setter Property="Padding"              Value="2,0" />
            <Setter Property="VerticalAlignment"    Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid x:Name="Root" Background="{TemplateBinding Background}"
                              CornerRadius="4">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="Root.Background"
                                                    Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="Root.Background"
                                                    Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <!-- Root border that looks like a text field -->
    <Border x:Name="RootBorder"
            Background="{ThemeResource TextControlBackground}"
            BorderThickness="0"
            CornerRadius="4"
            VerticalAlignment="Stretch">

        <Grid>

            <!-- Breadcrumb display mode -->
            <Grid x:Name="BreadcrumbView"
                  Visibility="Visible"
                  PointerPressed="OnBreadcrumbViewPressed">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Root arrow / quick-access menu button -->
                <Button x:Name="RootMenuButton"
                        Grid.Column="0"
                        Style="{StaticResource BreadcrumbChevronStyle}"
                        Width="28"
                        ToolTipService.ToolTip="Navigate to..."
                        Click="OnRootMenuClick">
                    <Image x:Name="RootMenuIcon" Width="16" Height="16" Stretch="UniformToFill" />
                </Button>

                <!-- Horizontal scroll viewer - clips and scrolls the chips -->
                <ScrollViewer x:Name="ChipScroller"
                              Grid.Column="1"
                              HorizontalScrollMode="Enabled"
                              HorizontalScrollBarVisibility="Hidden"
                              VerticalScrollBarVisibility="Disabled"
                              VerticalScrollMode="Disabled"
                              VerticalAlignment="Stretch">

                    <!-- Chips are built dynamically in code-behind and added here -->
                    <StackPanel x:Name="ChipPanel"
                                Orientation="Horizontal"
                                VerticalAlignment="Stretch"
                                Spacing="0" />

                </ScrollViewer>
            </Grid>

            <!-- Edit mode overlay: plain TextBox with no border/focus chrome + popup suggestions -->
            <Grid x:Name="EditPanel" Visibility="Collapsed">

                <!-- Custom-templated TextBox: no FocusedBorderElement, no blue line -->
                <TextBox x:Name="EditBox"
                         PlaceholderText="Type a path..."
                         VerticalAlignment="Stretch"
                         VerticalContentAlignment="Center"
                         FontSize="12"
                         FontFamily="Segoe UI Variable Text"
                         TextChanged="OnEditBoxTextChanged"
                         KeyDown="OnEditBoxKeyDown">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background"      Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding"         Value="6,0" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <!-- Minimal template: just the scroll host, no border elements at all -->
                                    <ControlTemplate TargetType="TextBox">
                                        <ScrollViewer x:Name="ContentElement"
                                                      HorizontalScrollMode="Auto"
                                                      HorizontalScrollBarVisibility="Hidden"
                                                      VerticalScrollMode="Disabled"
                                                      VerticalScrollBarVisibility="Disabled"
                                                      VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"
                                                      Background="Transparent"
                                                      AutomationProperties.AccessibilityView="Raw" />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Suggestion dropdown -->
                <Popup x:Name="SuggestPopup"
                       ShouldConstrainToRootBounds="False">
                    <Border Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4">
                        <ListView x:Name="SuggestList"
                                  MaxHeight="220"
                                  MinWidth="300"
                                  SelectionMode="Single"
                                  IsItemClickEnabled="True"
                                  ItemClick="OnSuggestItemClick"
                                  FontSize="12"
                                  FontFamily="Segoe UI Variable Text" />
                    </Border>
                </Popup>

            </Grid>

        </Grid>
    </Border>
</UserControl>
